[project]
name = "urbanvision-backend"
version = "0.1.0"
description = "UrbanVision Lab demo backend: FastAPI + YOLO11n inference and human-audit API"
requires-python = ">=3.11,<3.14"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "python-multipart>=0.0.9",
    "ultralytics>=8.3",
    "pillow>=10.0",
]

[tool.uv]
package = false
